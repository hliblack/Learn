### 在print()函数中使用 """...""" 主要是为了定义一个多行且能保留原始格式的字符串字面量。

print("""
      There's something wrong with your printer.
      With the three dobble quotes.
      We'll be able to type as much as we like.
      Even 4 lines if we want,or 5, or6
      """)

input("how old are you?\n")